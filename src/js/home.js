import { initAssets } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
  // Initializes favicon, logo, and updates the Wishlist badge counter from localStorage
  initAssets();
});